Development Session Summary -- 2026-05-18
============================================================

WHAT EXISTED AT SESSION START
------------------------------
Two nested git repos:
  - Outer: /mrmind/  (nearly empty, just contained MrMind_C/)
  - Inner: /mrmind/MrMind_C/  (all actual code)

Working code from prior sessions:
  - mrmind.py       -- ELIZA engine + Session logging
  - mrmind_llm.py   -- Claude API-powered version (claude-opus-4-7)
  - mrmind.yaml     -- ~80 rules drawn from the .g sources
  - conversations/  -- runtime chat transcripts
  - seshsums/       -- runtime session summaries

WHAT HAPPENED THIS SESSION
---------------------------
1. Fixed nested git repo situation.
   Ditched outer .git, moved MrMind_C/ contents up to repo root.
   Merged .claude/settings.local.json from both locations (inner had
   Python run permissions; outer had memory-read permissions).

2. Created README.md at repo root covering:
   - What MrMind is and the two versions
   - Dependencies and how to run
   - Logging directories (conversations/, seshsums/)
   - mrmind.yaml rule format
   - How to make changes (add rules, followups, tune stall behaviour)
   - Original source files

3. Read all original .g source files to inventory capabilities missing
   from the current reconstruction (see MISSING CAPABILITIES below).

FILES CHANGED
-------------
  README.md         -- created (new)
  seshsums/         -- moved to repo root (was inside MrMind_C/)
  .claude/settings.local.json -- merged permissions from both repos

MISSING CAPABILITIES (from original .g files, not yet in mrmind.py)
--------------------------------------------------------------------
These existed in the original weblab Gerbil-based system and were not
carried into the current ELIZA reconstruction:

a) 20 Questions game (MrMind/20Questions.g)
   A full structured multi-turn game: user thinks of a human attribute,
   MrMind asks yes/no questions probing it (does it involve family?
   is it constant? universal? necessary? unique to humans?).
   A real dialogue arc, not just pattern matching.

b) Human Quotient score (JB-added/HumanQuotient.g, ScoreMe.g,
   WinLoss.g, LexicalMath.g)
   A running numerical score tracking how "human" the user seems.
   LexicalMath.g implemented lexical increment/decrement up to 1000.
   Bot could comment on and update the score during conversation.

c) Repetition detection (JB-added/Repetition.g)
   Bot detected when user was repeating themselves and called it out.

d) Expression tracking (JB-added/ExpressionFilter.g,
   Defaults/defaults.g "Expressions repeater")
   Bot noted phrases/expressions the user used and could echo them
   back later in the conversation.

e) Probabilistic response selection (WantSomePointers.g "IfChance")
   Weighted-random selection from a pool of hints/suggestions, rather
   than strict cycling. Used when user was stuck.

f) User survey (MrMind/UserSurvey.g)
   Formal survey the bot could administer when user was stuck or
   explicitly asked for pointers. Referred to from multiple topics.

g) Focus/Subjects system (pervasive across all .g files) *** KEY MISSING FEATURE ***
   Every topic declares subjects: tags it belongs to (e.g. Subjects "ME","HELP").
   When a topic fires it can set the focus: Focus subjects "is your mother human?"
   On the next turn the engine preferentially tries topics whose subjects match
   the current focus before falling back to normal priority-order matching.
   Topics can check "If Focused then ..." to give shorter/different responses
   when they are the active focus.
   DontFocus clears the attention and returns to normal matching.
   This is the mechanism that lets MrMind pursue a line of inquiry across
   multiple turns without a fully scripted sequence -- the most impactful
   missing capability. The current followup: field is a crude one-level
   approximation; Focus/Subjects is the real thing.

IMPLEMENTED THIS SESSION
------------------------
Focus/Subjects system (mrmind.py + mrmind.yaml):

  mrmind.py changes:
    - MrMind.__init__: added self.focus = None
    - respond(): rule loop replaced with _find_match() call; on rule fire,
      sets self.focus from rule['focus'] or clears it if clear_focus: true
    - _find_match(): two-pass — focused rules first (subjects include
      self.focus), then all rules in normal priority order

  mrmind.yaml: 52 rules tagged across 7 subjects:
    BODY      — body/physical attributes (user_has_body sets focus)
    MIND      — thinking/cognition (user_can_think sets focus)
    EMOTIONS  — feelings/emotional life (user_has_emotions sets focus)
    FAMILY    — family relationships (user_has_mother/father set focus)
    CONVINCING — proving humanity (user_is_human sets focus)
    HUMANITY  — what it means to be human
    MORALS    — ethics/spirituality (user_has_morals sets focus)
    BOTS      — about MrMind's own nature
    clear_focus: profanity, never_mind, you_are_frustrating, want_hints

  All tests pass. Backward compatible — untagged rules unaffected.

NEXT STEPS
----------
- Consider 20 Questions game (multi-turn sequence topic)
- Consider Human Quotient score
- Update README with Focus/Subjects editing instructions ✓
